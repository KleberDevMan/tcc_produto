<!--<p id="notice"><%#= notice %></p>-->

<!--<h1>Teachers</h1>-->

<!--<table>-->
<!--  <thead>-->
<!--    <tr>-->
<!--      <th>Name</th>-->
<!--      <th colspan="3"></th>-->
<!--    </tr>-->
<!--  </thead>-->

<!--  <tbody>-->
<%# @teachers.each do |teacher| %>
<!--      <tr>-->
<!--        <td><%#= teacher.name %></td>-->
<!--        <td><%#= link_to 'Show', teacher %></td>-->
<!--        <td><%#= link_to 'Edit', edit_teacher_path(teacher) %></td>-->
<!--        <td><%#= link_to 'Destroy', teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
<!--      </tr>-->
<%# end %>
<!--  </tbody>-->
<!--</table>-->

<!--<br>-->

<%#= link_to 'New Teacher', new_teacher_path %>

<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-10">
      <div class="row align-items-center my-4">
        <div class="col">
          <h2 class="h3 mb-0 page-title"><%= t 'activerecord.models.teachers' %></h2>
        </div>
        <div class="col-auto">
          <a href="<%= new_teacher_path %>" class="btn btn-primary"><span class="fe fe-plus-circle fe-12 mr-2"></span>Adicionar</a>
        </div>
      </div>
      <div class="card shadow">
        <div class="card-body">
          <table class="table table-hover table-sm">
            <thead>
            <tr>
              <th>Nome</th>
              <th>Status</th>
              <th>Ação</th>
            </tr>
            </thead>
            <tbody>
            <% @teachers.each do |teacher| %>
              <tr>
                <td><strong><%= teacher.name %></strong></td>
                <td>
                  <% if teacher.status.active? %>
                    <span class="text-success">Ativo</span>
                  <% else %>
                    <span class="text-danger">Inativo</span>
                  <% end %>
                </td>
                <td>
                  <div class="dropdown">
                    <button class="btn btn-sm dropdown-toggle more-vertical" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      <span class="text-muted sr-only">Action</span>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dr<%= teacher.id %>">
                      <a class="dropdown-item" href="<%= edit_teacher_path teacher %>">Editar</a>
                      <a class="dropdown-item" href="<%= toggle_status_teacher_path teacher %>">
                        <% if teacher.status.active? %>
                          <span class="text-danger">Inativar</span>
                        <% else %>
                          <span class="text-success">Ativar</span>
                        <% end %>
                      </a>
                      <% if teacher.academic_works_count == 0 %>
                        <%= link_to 'Excluir', teacher, method: :delete, data: { confirm: t('notice.are_you_sure') }, class: 'dropdown-item bg-danger-dark' %>
                      <% end %>
                    </div>
                  </div>
                </td>
              </tr>
            <% end %>
            </tr>
            </tbody>
          </table>
        </div>
      </div>
      <%= paginate @teachers %>
    </div>
  </div>
</div>
